// Exercise 3

var personName = "Eric";

// Lowercase
alert(personName.toLowerCase());

// Uppercase
alert(personName.toUpperCase());

// Capitalize
alert(personName.charAt(0).toUpperCase() + personName.slice(1).toLowerCase());
